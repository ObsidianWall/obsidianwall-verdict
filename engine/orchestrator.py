# engine/orchestrator.py

# Responsibilities:
      # load policy,
      # normalize policy,
      # validate policy,
      # build runtime context,
      # invoke evaluator,
      # invoke analyzers,
      # invoke recommender,
      # construct audit artifact.

   # This becomes:

       # the execution coordinator.